{
  "name": "mycelium",
  "version": "1.0.0",
  "description": "E2E encrypted nerve network for Claude Code instances — PFS, TOFU, signatures, replay-proof",
  "engines": {
    "bun": ">=1.3.5"
  },
  "scripts": {
    "relay": "bun run relay.ts",
    "test": "bun run test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.0",
    "tweetnacl": "^1.0.3",
    "tweetnacl-util": "^0.15.1",
    "zod": "^3.23.0"
  }
}
